export const ReviewMode = ({ progress }: { progress: string }) => (
  <section className="card">
    <h3 style={{ marginTop: 0 }}>Review Mode</h3>
    <p style={{ margin: 0 }}>Progress: {progress}</p>
  </section>
);
